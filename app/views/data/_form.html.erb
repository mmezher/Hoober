<%= simple_form_for(@datum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.attachment_field :csv %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
